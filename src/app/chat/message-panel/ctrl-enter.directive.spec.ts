import { CtrlEnterDirective } from './ctrl-enter.directive';

describe('CtrlEnterDirective', () => {
  it('should create an instance', () => {
    const directive = new CtrlEnterDirective();
    expect(directive).toBeTruthy();
  });
});
